# Ignore optional peer dependencies
# This prevents React Native dependencies from being included
# since they're only optional peer dependencies from Firebase
legacy-peer-deps=false
strict-peer-deps=false
